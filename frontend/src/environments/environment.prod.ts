export const environment = {
  production: true,
  // Angular is browser-side; API URL must be reachable from the user's browser.
  // In production this would be the real domain (e.g. https://api.skyroute.com).
  apiUrl: 'http://localhost:5000/api'
};
